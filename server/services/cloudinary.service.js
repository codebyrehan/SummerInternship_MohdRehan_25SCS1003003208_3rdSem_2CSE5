// Cloudinary service stubbed — cloudinary removed
export const uploadImage = async () => ({ url: '', publicId: '' });
export const deleteImage = async () => {};
export default { uploadImage, deleteImage };
